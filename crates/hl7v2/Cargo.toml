[package]
name = "hl7v2"
version = "0.1.0"
description = "Dependency-free HL7 v2.x message parser with byte spans for every field, repetition, component and subcomponent"
keywords = ["hl7", "healthcare", "parser", "ihe", "dicom"]
categories = ["parser-implementations", "science"]
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]

[dev-dependencies]
